export default function SearchPlaceholderPage() {
  return (
    <main className="mx-auto max-w-3xl px-6 py-16">
      <h1 className="text-3xl">Search programs</h1>
      <p className="mt-4 text-[var(--color-text-muted)]">
        Filter UI ships in Sprint 2. Data model supports 12 categories, 3
        admission types, explicit format column, gotcha flags, and
        &ldquo;Contact program&rdquo; price handling.
      </p>
    </main>
  );
}
